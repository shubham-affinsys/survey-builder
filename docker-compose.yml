services:
  # postgres:
  #   image: postgres:17  
  #   container_name: postgres-db
  #   environment:
  #     POSTGRES_USER: shubh
  #     POSTGRES_PASSWORD: 9504
  #     POSTGRES_DB: survey_db
  #     POSTGRES_PORT: 5433 
  #   ports:
  #     - "5432:5432" 
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data  

  survey-builder:
    image: survey-builder:v2
    container_name: survey-svc
    environment:
      POSTGRES_USER: shubh
      POSTGRES_PASSWORD: 9504
      POSTGRES_DB: survey_db
      POSTGRES_HOST: postgres-db
      POSTGRES_PORT: 5432
      RAILWAY_PG_URL: "postgresql://postgres:xmQXiOJdGgbtGfnfsislJVqlxafWQwGj@autorack.proxy.rlwy.net:25681/railway"
      PROCESSES: 2  
      WORKERS: 1         
      FAST_MODE: "false"
      LOG_LEVEL: "INFO"
    ports:
      - "8081:8080"
    # depends_on:
    #   - postgres
    networks:
      - survey-network
networks:
  survey-network:  # Use the default bridge network
    external: true 


# volumes:
#   postgres_data:
